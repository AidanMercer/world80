background_opacity 0.4
dynamic_background_opacity yes

# per-theme colors, regenerated by ~/dotfiles/.config/hypr/theme-colors.sh on
# every theme switch; kitty re-reads this on SIGUSR1.
include ${HOME}/.cache/theme/kitty.conf
